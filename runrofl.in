/*****
 * runrofl.in
 *
 * Runtime functions for rofl type
 *****/

TcpServer* => primTcpServer()
TcpSocket* => primTcpSocket()

#include "rofl.h"

using namespace camp;
using namespace settings;
using namespace vm;

// Autogenerated routines:

void print(string s)
{
  printf("%s", s.c_str());
}

TcpServer *create_server(string host, Int port)
{
  return create_server(host, port);
}

TcpSocket *server_accept(TcpServer *s) 
{
  return server_accept(s);
}

Int server_close(TcpServer *s) 
{
  return server_close(s);
}

string socket_read(TcpSocket *s) 
{
  return socket_read(s);
}

Int socket_write(TcpSocket *s, string d) 
{
  return socket_write(s, d);
}

Int socket_close(TcpSocket *s) 
{
  return socket_close(s);
}
